{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Solidity smart contracts for CrossChain Swap System",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat ignition deploy ignition/modules/deploy.ts --network sepolia",
    "deploy:mumbai": "hardhat ignition deploy ignition/modules/deploy.ts --network mumbai",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
