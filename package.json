{
  "name": "crosschain-swap-system",
  "version": "1.0.0",
  "description": "Cross-chain token swap platform using LayerZero bridge protocol",
  "private": true,
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "contracts:compile": "npm run compile --workspace=contracts",
    "contracts:test": "npm run test --workspace=contracts",
    "backend:dev": "npm run dev --workspace=backend",
    "frontend:dev": "npm run dev --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
